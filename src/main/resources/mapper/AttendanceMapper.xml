<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.AttendanceMapper">

    <!-- 插入签到记录 -->
    <insert id="insertAttendance" parameterType="com.example.demo.model.Attendance">
        INSERT INTO attendance (activity_id, user_id, sign_time, sign_type)
        VALUES (#{activityId}, #{userId}, #{signTime}, #{signType})
    </insert>

    <!-- 根据活动ID查询签到记录 -->
    <select id="findByActivityId" resultType="com.example.demo.model.Attendance">
        SELECT id, activity_id, user_id, sign_time, sign_type
        FROM attendance
        WHERE activity_id = #{activityId}
    </select>

    <!-- 根据用户ID查询签到记录 -->
    <select id="findByUserId" resultType="com.example.demo.model.Attendance">
        SELECT id, activity_id, user_id, sign_time, sign_type
        FROM attendance
        WHERE user_id = #{userId}
    </select>

    <!-- 按活动ID和签到状态筛选签到记录 -->
    <select id="findByActivityIdAndStatus" resultType="com.example.demo.model.Attendance">
        SELECT id, activity_id, user_id, sign_time, sign_type
        FROM attendance
        WHERE activity_id = #{activityId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
    </select>
</mapper>