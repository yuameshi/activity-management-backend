<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.RegistrationMapper">

  <resultMap id="RegistrationResultMap" type="com.example.demo.model.Registration">
    <id property="id" column="id"/>
    <result property="userId" column="user_id"/>
    <result property="activityId" column="activity_id"/>
    <result property="registerTime" column="register_time"/>
    <result property="status" column="status"/>
  </resultMap>

  <insert id="insertRegistration" parameterType="com.example.demo.model.Registration" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO registration (user_id, activity_id, register_time, status)
    VALUES (#{userId}, #{activityId}, #{registerTime}, #{status})
  </insert>

  <select id="findById" parameterType="long" resultMap="RegistrationResultMap">
    SELECT * FROM registration WHERE id = #{id}
  </select>

  <select id="findByUserAndActivity" resultMap="RegistrationResultMap">
    SELECT * FROM registration WHERE user_id = #{userId} AND activity_id = #{activityId} LIMIT 1
  </select>

  <select id="findByActivityId" parameterType="long" resultMap="RegistrationResultMap">
    SELECT * FROM registration WHERE activity_id = #{activityId} ORDER BY register_time DESC
  </select>

  <select id="findByUserId" parameterType="long" resultMap="RegistrationResultMap">
    SELECT * FROM registration WHERE user_id = #{userId} ORDER BY register_time DESC
  </select>

  <update id="updateRegistration" parameterType="com.example.demo.model.Registration">
    UPDATE registration
    <set>
      <if test="userId != null">user_id = #{userId},</if>
      <if test="activityId != null">activity_id = #{activityId},</if>
      <if test="registerTime != null">register_time = #{registerTime},</if>
      <if test="status != null">status = #{status},</if>
    </set>
    WHERE id = #{id}
  </update>

  <delete id="deleteById">
    DELETE FROM registration WHERE id = #{id}
  </delete>

  <delete id="deleteByUserAndActivity">
    DELETE FROM registration WHERE user_id = #{userId} AND activity_id = #{activityId}
  </delete>

</mapper>